[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "deepagent"
version = "0.7.1"
description = "Rust coding agent powered by Gemini Flash 3.1, installable via pip"
requires-python = ">=3.10"
license = "MIT"

[tool.maturin]
bindings = "bin"
strip = true
